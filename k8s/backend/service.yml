apiVersion: v1
kind: Service
metadata:
  name: todo-api-service 
  namespace: todo-ns
spec:
  type: ClusterIP
  selector:
    app: todo-api-app
  ports:
   - port: 4000
     targetPort: 4000
     protocol: TCP